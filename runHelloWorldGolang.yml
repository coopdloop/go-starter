---
- hosts: localhost
  connection: local
  tasks:
    - name: cp golang binary
      copy:
        src: /usr/local/go/bin/go
        dest: /usr/local/go/bin/go
    - name: Run scripts hosts
      script: /usr/local/go/bin/go run hello-world.go